{
    "name": "order_query",
    "description": "用户查询订单状态、物流信息、订单详情时的业务处理流程",
    "content": """
订单查询服务工作链路（必须逐步执行，不可跳过）：

1. 确认用户是否提供了订单号：
   - 有订单号 → 调用 query_order(order_id) 查询单个订单。
   - 无订单号 → 调用 query_my_orders() 返回用户的所有订单列表。
2. 如果订单不存在，告知用户并请其确认订单号。
3. 如果订单存在，将信息以清晰格式返回，包含：订单状态、金额、收货地址、创建时间。
4. 如果用户询问物流详情，查看订单中的 logistics_status 字段并告知。
5. 所有查询操作只读不写，不修改任何数据。
"""
}