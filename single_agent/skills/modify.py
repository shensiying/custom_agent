{
    "name": "order_modify",
    "description": "用户修改订单收货地址、收件人姓名或联系电话时的业务处理流程",
    "content": """
修改订单信息服务工作链路（必须逐步执行，不可跳过）：

1. 确认用户提供了订单号。若未提供，提问："请提供需要修改的订单号。"
2. 确认用户想要修改的具体字段（地址 / 收件人姓名 / 电话）。若未明确，提问："请问您要修改收货地址、收件人姓名还是联系电话？"
3. 调用 query_order(order_id) 查询订单当前状态。
4. 通过RAG检索相关业务规则，根据召回的业务规则判断是否允许修改：
   - 仅当订单状态为 pending 或 paid 时允许修改。
   - 若状态为其他值（如 shipped、delivered），告知用户无法修改并说明原因。
5. 规则通过后，向用户确认要修改的内容和新值，等待用户确认。
6. 用户确认后：
   - 修改地址 → 调用 change_address(order_id, new_address)
   - 修改收件人 → 调用 change_receiver_info(order_id, name=..., phone=...)
7. 修改成功后告知用户具体变更内容。
8. 若用户同时修改多个字段，逐项完成。
"""
}